# Telegram bot
BOT_TOKEN=your-telegram-bot-token-here

# PostgreSQL (used by docker-compose.prod.yml)
POSTGRES_PASSWORD=change-me-to-a-strong-password

# App / Alembic (override only if not using docker-compose.prod.yml defaults)
# DATABASE_URL=postgresql+asyncpg://perekup:<password>@db:5432/perekup_helper
# REDIS_URL=redis://redis:6379/0

# AI categorization
ANTHROPIC_API_KEY=sk-ant-...

# Parser interval (minutes)
CHECK_INTERVAL_SECONDS=300
